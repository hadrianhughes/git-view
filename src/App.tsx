import React, { StatelessComponent as C } from 'react';
import GlobalStyles from './styles/global';

const App = (): C => (
  <div>
    <GlobalStyles />
    Hello World!
  </div>
);

export default App;
